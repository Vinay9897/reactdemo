

const Addition = (a, b) => {
    return a + b;
}

const Multiplication = (a, b) => {

    return a * b;
}

const Division = (a, b) => {
    // if (b > 0) {
    return a / b;
    // }
    // else {
    //     console.log("b's value should be greater than zero");
    //     return;
    // }
}

export { Addition, Multiplication, Division };
